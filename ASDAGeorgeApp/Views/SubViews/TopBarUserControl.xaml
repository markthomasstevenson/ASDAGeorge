<UserControl x:Class="ASDAGeorgeApp.Views.SubViews.TopBarUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Margin="10 0 25 0"
                               Text="Hi, Jessica" />

            <TextBlock Grid.Column="1" Margin="25 0 25 0"
                               Text="Here's your Wishlist" />

            <Border Grid.Column="0" Style="{StaticResource Underline}" Margin="0 0 5 -5" />

            <Border Grid.Column="1" Style="{StaticResource Underline}" Margin="5 0 5 -5"  />
        </Grid>
    </Grid>
</UserControl>
